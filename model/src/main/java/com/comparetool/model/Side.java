package com.comparetool.model;

/**
 * Identifies which pane of the side-by-side comparison is referenced.
 */
public enum Side {
    /** The left (first-selected) pane. */
    LEFT,
    /** The right (second-selected) pane. */
    RIGHT
}
